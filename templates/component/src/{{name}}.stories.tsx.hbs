import { Meta, Story } from '@storybook/react';

import { {{capitalize name}}, {{capitalize name}}Props } from './{{name}}';

const config: Meta = {
  title: '{{capitalize name}}',
  component: {{capitalize name}}
};
export default config;

const Template: Story<{{capitalize name}}Props> = (args) => <{{capitalize name}} {...args} />;

export const Default = Template.bind({});
Default.args = {};
