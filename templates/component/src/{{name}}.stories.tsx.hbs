import type { Meta, StoryObj } from '@storybook/react';

import { {{capitalize name}}, {{capitalize name}}Props } from './{{name}}';

type Story = StoryObj<{{capitalize name}}Props>;

export const Default: Story = {
  args: {}
};

const meta: Meta<{{capitalize name}}Props> = {
  title: '{{capitalize name}}',
  component: {{capitalize name}}
};

export default meta;
