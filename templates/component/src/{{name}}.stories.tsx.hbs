import { Meta, Story } from '@storybook/react';
import React from 'react';

import { {{normalize name}}, {{normalize name}}Props } from './{{name}}';

const config: Meta = {
  title: '{{normalize name}}',
  component: {{normalize name}}
};
export default config;

const Template: Story<{{normalize name}}Props> = (args) => <{{normalize name}} {...args} />;

export const Default = Template.bind({});
Default.args = {};
