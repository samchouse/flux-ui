import React, { forwardRef } from 'react';

export interface {{capitalize name}}Props {}

export const {{capitalize name}} = forwardRef<unknown, {{capitalize name}}Props>(
  ({ ...props }: {{capitalize name}}Props, ref) => <></>
);

{{capitalize name}}.displayName = '{{capitalize name}}';
