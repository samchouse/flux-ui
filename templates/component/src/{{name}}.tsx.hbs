import React, { forwardRef } from 'react';

export interface {{normalize name}}Props {}

export const {{normalize name}} = forwardRef<unknown, {{normalize name}}Props>(
  ({ ...props }: {{normalize name}}Props, ref) => <></>
);

{{normalize name}}.displayName = '{{normalize name}}';
