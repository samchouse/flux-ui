import '@testing-library/jest-dom';
import { cleanup } from '@testing-library/react';

import { {{capitalize name}}, {{capitalize name}}Props } from './{{name}}';

describe('@flux-ui/{{name}}', () => {
  afterEach(cleanup);
});
